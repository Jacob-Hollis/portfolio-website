version: '3.9'

services:
  app:
    build:
      context: .
    command: sh -c "python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./website:/website
      - ./data/web:/vol/web
    ports:
      - 8000:8000
    environment:
      - DJANGO_DEBUG=1